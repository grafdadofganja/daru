<?php

public function actionReligion()
{
    $model = new app\models\sqlite\Religions();

    if ($model->load(Yii::$app->request->post())) {
        if ($model->validate()) {
            // form inputs are valid, do something here
            return;
        }
    }

    return $this->render('religion', [
        'model' => $model,
    ]);
}